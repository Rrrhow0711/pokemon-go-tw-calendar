# 可選：本機預覽網址。GitHub Actions 會由 GITHUB_REPOSITORY 自動推導。
SITE_BASE_URL=http://localhost:4173/

# 可選：更換 GitHub Models 模型。Actions 使用內建 GITHUB_TOKEN，不需另建 API Key。
GITHUB_MODEL=openai/gpt-4.1-mini

# 可選：判定來源筆數異常下降的比例（0 到 1）。
MIN_SOURCE_RATIO=0.5
